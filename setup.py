from setuptools import setup, find_packages

setup(
    name="token-refresh-service",
    version="0.1.0",
    packages=find_packages(),
    install_requires=[
        # Las dependencias se manejarán con requirements.txt
    ],
)
